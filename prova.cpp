#include <iostream>
#include "memory.hpp"
#include <iomanip>
#include <cstdint>

using namespace std;

int main()
{
    /*Memory ram(1024); //create a memory with 1024 bytes

    //creo un vettore di dati da scrivere in memoria

    std::vector<uint8_t> data= { 0x00, 0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x08, 0x09, 0x0A, 0x0B, 0x0C, 0x0D, 0x0E, 0x0F };

    //scrivo i dati in memoria
    ram.setData(data);

    //leggo il primo byte
    cout << "Byte 0: " << hex << (int)ram.readByte(0) << endl;

    //leggo il primo word
    cout << "Word 1: " << hex << std::setw(4) << std::setfill('0')<< ram.readWord(1) << endl;

    //leggo il primo dword
    cout << "DWord 3: " << hex << std::setw(8) << std::setfill('0')<< ram.readDWord(3) << endl;

    //leggo il primo qword
    cout << "QWord 0: " << hex << std::setw(16) << std::setfill('0')<< ram.readQWord(0) << endl;
    
    //ottengo a grandezza della memoria
    cout << "Size: " << dec << ram.getSize() << endl;

    //OTTENGONO I DATI DELLA MEMORIA
    const std::vector<uint8_t>& data2 = ram.getData();

    //stampo i dati della memoria
    for (size_t i = 0; i < data.size(); i++)
    {
        cout << "Data[" << i << "]: " << hex << std::setw(2) << std::setfill('0') << (int)data2[i] << endl;
    }

    //puliscono la memoria
    ram.clear();

    //ottengo i dati della memoria
    const std::vector<uint8_t>& data3 = ram.getData();

    //stampo i dati della memoria
    for (size_t i = 0; i < data.size(); i++)
    {
        cout << "Data[" << i << "]: " << hex << std::setw(2) << std::setfill('0')<< (int)data3[i] << endl;
    }*/






};